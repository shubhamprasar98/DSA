class Solution {
    int maxPathSum(Node root) {
        // code here
        if(root == null) return 0;
        
        int[] sum = new int[1];
        
        sum[0] = Integer.MIN_VALUE;
        
        int val = f(root, sum);
        
        if(sum[0] == Integer.MIN_VALUE) return val;
        
        return sum[0];
        
    }
    
    private int f(Node root, int[] sum) {
        
        if(root == null) return 0;
        
        int leftSum = f(root.left, sum);
        int rightSum = f(root.right, sum);
        
        if(root.left != null && root.right != null) {
            
            int total = leftSum + rightSum + root.data;
        
            sum[0] = Math.max(total, sum[0]);
        
            return Math.max(leftSum, rightSum) + root.data;
            
        }
        
        return root.data + ((root.left == null) ? rightSum : leftSum);
    }
}